# mod-playerbot 测试协助文档

> 本文件记录这一版改动需要回归的功能点,方便以后对照测试。测完一项可在 `[ ]` 打勾。

## 一、测试环境准备

### 账号

| 角色 | 数量 | 用途 |
| --- | --- | --- |
| GM 账号(GM3) | 1 | 执行 `/bot set` 标记机器人 |
| Master(普通玩家) | 1 | 作为主人带队 |
| 机器人 A(建议战士) | 1 | 测试近战 |
| 机器人 B(建议法师) | 1 | 测试远程 |

### 初始化步骤

1. GM 登录,执行:
   ```text
   /bot set <机器人A名字>
   /bot set <机器人B名字>
   ```
2. 两个机器人分别登录,确认聊天框收到 `[Bot] Bot mode active.` 提示。
3. Master 分别邀请两个机器人进组。
4. 机器人在各自客户端**手动点确认**进组。
5. Master 执行 `/bot list` 确认两个机器人的 Master 都是队长。

---

## 二、测试用例

### 1. 组队:不自动接受邀请

- [ ] Master 发起组队邀请后,机器人**不会**秒进组,客户端会弹出邀请框。
- [ ] 只有手动点确认后,机器人才进组。
- [ ] 进组后队长自动成为机器人的 Master(`/bot list` 可验证)。

### 2. `/bot stance` 权限(Master 可用)

- [ ] Master(普通玩家)执行 `/bot stance d`,应正常执行,**不应提示「没有这个命令」**。
- [ ] Master 执行 `/bot set xxx`(GM 专属),应提示无权限。

### 3. `/bot stance` 批量与单个

- [ ] `/bot stance d` → 名下**所有**机器人切换为 Defensive。
- [ ] `/bot stance p` → 名下所有机器人切换为 Passive。
- [ ] `/bot stance a` → 名下所有机器人切换为 Aggressive。
- [ ] `/bot stance <机器人A名字> aggressive` → 仅该机器人切换为 Aggressive,另一个不变。
- [ ] 简写与完整名等价:`d`=`defensive`、`p`=`passive`、`a`=`aggressive`。
- [ ] 不带参数 `/bot` 能显示帮助信息。

### 4. 近战进战距离(战士主动冲锋)

- [ ] Master 远程拉一只远处的怪。
- [ ] 战士应**主动冲向怪**,而不是站在原地等怪走到跟前。
- [ ] 法师应在约 30 码外**远程开打**。

### 5. 多机器人跟随分散站位

- [ ] 两个机器人跟随同一 Master 站定时,位置**不重叠**,分散在 Master 身后。
- [ ] 移动过程中也能保持分散。

### 6. 跨地图跟随

- [ ] Master 使用炉石/传送前往另一张地图后,机器人**自动传送**跟过去。
- [ ] Master 进副本后,机器人进入**同一个副本实例**(不是各自开一个新副本)。
- [ ] Master 进战场/竞技场时,机器人**不**自动跟随(预期不跟随)。

### 7. 基础行为回归

- [ ] 机器人跟随 Master 移动。
- [ ] Master 战斗时机器人协助攻击(防御姿态下)。
- [ ] Master 离线约 5 分钟后,机器人自动离队并清除 Master。
- [ ] 机器人死亡/被眩晕等异常状态时不会乱跑。

---

## 三、注意事项

1. **命令只查找在线玩家**:`/bot set`、`/bot stance` 等命令的 `$name` 必须是**当前在线**的玩家。
2. **战场/竞技场**:机器人不会自动跟随 Master 进入,属预期行为。
3. **编译警告**:编译时有两个 `unused parameter 'analysis'` 警告,属无害(参数暂未使用),不影响运行。
4. **副本跟随的前提**:机器人需与 Master 在**同一队伍**,且 Master 是队长,才能进入同一副本实例。
5. **站立分散**按机器人 GUID 计算,不同机器人的位置固定但彼此不同。

---

## 四、回归速查表

| 功能 | 关键命令/操作 | 预期 |
| --- | --- | --- |
| 组队手动确认 | Master 邀请机器人 | 需手动确认,不自动进组 |
| stance 权限 | Master 输 `/bot stance d` | 不提示"没有这个命令" |
| stance 批量 | `/bot stance d/p/a` | 名下所有机器人一起改 |
| stance 单个 | `/bot stance 名字 aggressive` | 只改指定机器人 |
| 近战冲锋 | Master 拉怪 | 战士主动冲上去 |
| 远程输出 | Master 拉怪 | 法师 30 码外开打 |
| 分散站位 | 2 机器人跟随 | 不重叠 |
| 跨地图 | Master 传送 | 机器人自动跟随 |
| 副本 | Master 进副本 | 机器人进同一实例 |

---

## 五、v2.2.0 自动施法/战斗优化 回归测试点

> 本轮改动（编译时间 2026-08-19 00:11:38）新增/变更的自动施法与战斗行为，重点回归。

### 1. 自动施法技能分类

- [ ] **自解控不乱放**：人类机器人有「自利」时，平时**不会**乱放；被昏迷/恐惧/定身/沉默时**自动解控**。
- [ ] **净化不乱放**：矮人机器人有「石化形态」时，平时不乱放；中流血/疾病时施放。
- [ ] **治疗不乱放**：带治疗技能的机器人（如圣光术），目标血量 **≥80% 不施放**；目标血量 <80% 才治疗。
- [ ] **施法防打断**：机器人读条（如火球）时，**不会**每 tick 切别的技能打断自己读条。

### 2. 角色分工

- [ ] **明显 T 走坦克逻辑**：带盾+防护天赋的战士/圣骑，战斗中主动嘲讽/拉仇恨/减伤，**不把技能条当输出乱放**。
- [ ] **无 T 时奶转 DPS**：队伍无明确 T 时，治疗职业机器人转为输出（技能条自动施放），不做专职治疗。

### 3. 追击/搜索（参考宠物）

- [ ] **离 Master 过远停止**：机器人追击目标离 Master 超过约 **90 码**时停止追击，返回跟随 Master。
- [ ] **aggressive 主动搜索**：主动姿态下搜索 45 码内最近敌对目标（同宠物，不因距离乱引怪）。

### 4. 追加回归速查

| 功能 | 操作/现象 | 预期 |
| --- | --- | --- |
| 自解控 | 机器人被晕/恐惧 | 自动解控（自利等） |
| 净化 | 机器人中流血/疾病 | 自动施放净化（石化形态等） |
| 治疗阈值 | 队友/自己满血 | 不施放治疗技能 |
| 施法防打断 | 机器人读条中 | 不切技能打断读条 |
| T 分工 | 带盾防护机器人 | 嘲讽/拉仇恨/减伤 |
| 无T奶转DPS | 队伍无T时治疗职业 | 转输出（技能条） |
| 追击停止 | 目标离 Master 过远 | 停止追击、回跟随 |
| 命令输出格式 | `/bot list` | 输出 `BOTLIST;/BOT;` 标准格式（分号分隔，无颜色码） |

---

## 五、运行版问题回归用例（正在运行服务）

> 「运行版」= 当前 worldserver 实际加载的版本（`modules/mod-playerbot` 编译产物）。
> 以下按**运行版解决的每个问题**设计回归用例。测试架构：**client 普通玩家 = Master 观察者，SOAP/GM = 控制端**。

### R1. 宠物式 AI 主循环 + 远程保距 + 击杀即时回跟随

| 用例 | 验证点 | 步骤 | 预期结果 |
|------|--------|------|----------|
| R1-T1 | 远程保距 | SOAP `bot set` 法师；Master 拉怪 | 法师在 ~30 码外远程输出，不贴近近战距离 |
| R1-T2 | 击杀回跟随 | Master 击杀目标后 | 机器人**立即停止攻击**、回到 Master 身边跟随 |
| R1-T3 | 主循环稳定 | 连续多场战斗 | bot 不卡死、不脱战乱跑、命令状态正确 |

### R2. 击杀转火 + 猎人自动射击

| 用例 | 验证点 | 步骤 | 预期结果 |
|------|--------|------|----------|
| R2-T1 | 击杀转火 | 目标 A 死后附近有目标 B | bot 自动转火 B，不空站 |
| R2-T2 | 猎人自动射击 | 猎人 bot 有弹药打怪 | 自动触发 Auto Shot（远程自动射击） |
| R2-T3 | 无目标回跟随 | 全部目标死亡 | bot 清战斗状态回跟随 |

### R3. 施法前转身 + 客户端目标设置

| 用例 | 验证点 | 步骤 | 预期结果 |
|------|--------|------|----------|
| R3-T1 | 施法转身 | bot 被目标侧击/背对时 | 施法/攻击前**转身面向目标**（朝向对准） |
| R3-T2 | 目标设置 | 战斗中查 client 目标 | `UNIT_FIELD_TARGET` 指向当前攻击目标 |

### R4. 宠物式目标选择

| 用例 | 验证点 | 步骤 | 预期结果 |
|------|--------|------|----------|
| R4-T1 | 锁定目标 | bot 攻击中，其他怪打 bot | bot **不切换**目标，继续当前目标 |
| R4-T2 | 目标死亡切换 | 锁定目标死亡 | 此时才切换到新目标 |

### R5. ATTACK 命令目标死后重选

| 用例 | 验证点 | 步骤 | 预期结果 |
|------|--------|------|----------|
| R5-T1 | 死后重选 | `.bot attack X`，X 死亡 | bot 落到**姿态段重新选目标**（对齐 PetAI::KilledUnit） |
| R5-T2 | 命令清理 | X 死亡且无新目标 | attack 命令自动清理，bot 回跟随（同宠物） |

### R6. 12槽技能条 + 全自动施放 + 队友向技能 + 保持原位不追

| 用例 | 验证点 | 步骤 | 预期结果 |
|------|--------|------|----------|
| R6-T1 | 技能条直读 | 角色第一技能条放技能 | bot 直读该技能条并**全自动施放** |
| R6-T2 | 队友向技能 | 队伍中有队友掉血/需 buff | 治疗/增益类技能**对队友施放**（队友向判断） |
| R6-T3 | 保持原位不追 | `.bot stay` 后目标跑远 | bot 停在原地，**不追击**（完全模仿宠物） |

> 验证途径：SOAP（GM）执行 `bot set/list` + `.bot attack/stay`（玩家命令需真客户端）；行为观察经服务器日志 + Master(client) 视角。

---

## 六、仓库与版本说明（2026-08-19 统一）

> ⚠️ **历史混乱已修正**：曾存在两个 mod-playerbot 仓库线，导致 GitHub 误推过旧版。
> 现已统一：**`modules/mod-playerbot`（本目录）为唯一权威开发/编译/推送源**。

| 项 | 说明 |
|----|------|
| **权威源码** | `modules/mod-playerbot`（本目录），编译 worldserver 实际使用 |
| **GitHub** | 统一推送本目录内容到 `zj0882cn/mod-playerbot` main |
| **运行版** | 本目录编译产物（GUID 协议已验证：`BOTSET;Player-0-00000001;...`）|
| **已实现功能** | GUID 协议、法力检查(P-006)、增益去重(P-007)、弹药检查(P-008)、自解、净化、打断预防、治疗阈值、角色分工(T/无T奶转DPS)、`GroupHasTank()`、PB_LOG 调试日志(`PlayerBot.DebugLevel` 0-3) |
| **旧副本** | `/workspace/mod-playerbot`（另一 git 线，内容过时，已停止作为开发/推送源）|

---

## 七、死亡与距离场景回归（R7-R9）

> 依据源码逻辑设计（`BotPlayerScript.cpp`）。验证架构：client 普通玩家=Master，SOAP/GM=控制端，日志/状态经 SOAP + 服务器日志确认。

### R7. Master 死亡场景（源码 L170-191）

| 用例 | 场景 | 步骤 | 预期结果 |
|------|------|------|----------|
| R7-T1 | 战斗中死亡 | Master 战斗中死亡 | bot **立即停止战斗**（`CombatStop`）、脱战 |
| R7-T2 | 战斗中死亡后 | 同上，观察 bot | bot 停止攻击当前目标，**回到跟随 Master** |
| R7-T3 | 意外死亡 | Master 坠崖/溺水/被秒 | bot 同样停战回跟随，不卡死 |
| R7-T4 | 死亡后跟随 | Master 死亡状态 | bot 仍执行跟随（跟随死亡目标/灵魂）|
| R7-T5 | 队长转移 | Master 死亡后队长换新角色（存活）| bot 的 **Master 切换到新队长**（`SetMaster`）|

### R8. Master 距离场景（`UpdateFollow` L576-607 + 跨图 L217-232）

| 用例 | 距离 | 步骤 | 预期结果 |
|------|------|------|----------|
| R8-T1 | 近距离(<2000码) | Master 移动 | bot 正常**跑动跟随**（MoveFollow），不瞬移 |
| R8-T2 | 远距离(>2000码) | Master 跑远 | bot **宠物式召回传送**回身边（非战斗时）|
| R8-T3 | 跨地图 | Master 换地图 | bot **传送跟随**到 master 身边 |
| R8-T4 | 跨副本 | Master 进副本(同队) | bot 传送进**同一副本实例** |
| R8-T5 | 战场/竞技场 | Master 进战场 | bot **不跟随**（预期行为）|
| R8-T6 | 追击过远 | bot 追击目标离 Master >90码 | 停止追击，**返回跟随** |
| R8-T7 | 边界~2000码 | Master 在约 2000 码处 | 正常运行，不误触发传送 |

### R9. Bot 死亡场景（源码 L193-215）

| 用例 | 状态 | 步骤 | 预期结果 |
|------|------|------|----------|
| R9-T1 | 跟随中死亡 | bot 跟随途中被杀 | 自动去墓地**满血复活** → 回跟随 |
| R9-T2 | 战斗中死亡 | bot 战斗死亡 | 自动复活，恢复职责 |
| R9-T3 | 停留中死亡 | bot stay 状态死亡 | 复活后**回到停留位置** |
| R9-T4 | 攻击中死亡 | bot attack 状态死亡 | 复活后按命令状态恢复 |
| R9-T5 | Master 不在 | Master 离线/不在世界 | bot **不自动复活**（等待，`if(master...IsInWorld())`）|
| R9-T6 | 复活条件 | 观察 | 复活仅当 **Master 存活且在世界上**时触发 |
| R9-T7 | 复活流程 | 观察日志 | `BuildPlayerRepop→RepopAtGraveyard→ResurrectPlayer(满血)→SpawnCorpseBones` |

> 验证途径：`R7/R8` 大部分可经 SOAP（bot set/list 状态）+ worldserver 日志（PB_LOG）观察；
> 战斗/死亡场景需 client 普通玩家（Master）拉怪触发；`R9` 复活流程看日志关键词 `auto-revived at graveyard`。
